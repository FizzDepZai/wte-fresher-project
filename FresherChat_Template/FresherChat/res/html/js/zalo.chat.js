(function (a, b) {
    a.friendChat = {
        nodeContentRoot: $("#right-content #conversation-content"),      
        sendMsg: function (message) {
            
        },
        receiveMsg: function (message) {

        }
    };
} (zav, window));